/**
 * Supabase's generated `Database` shape, re-exported from `@luhive/db`.
 *
 * supabase-js infers `.from()` results from this shape, so it cannot be
 * replaced by the Kysely types in `@luhive/db` — both are generated from the
 * same schema. Import entity types from `~/shared/models/entity.types`; this
 * module is only for typing the Supabase client and its query payloads.
 */
export * from "@luhive/db/supabase";
